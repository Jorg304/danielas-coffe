export interface ICoffe {
    id:             number;
    text:           string;
    kindcoffe:      string;
    description:    string;
    outOfStock:     boolean;
}

export interface IActivate {
    activate:       boolean;
    index:          number;
}